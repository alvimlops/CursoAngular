import { HeghlightDirective } from './heghlight.directive';

describe('HeghlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HeghlightDirective();
    expect(directive).toBeTruthy();
  });
});
