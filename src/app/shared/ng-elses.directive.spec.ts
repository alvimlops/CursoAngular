import { NgElsesDirective } from './ng-elses.directive';

describe('NgElsesDirective', () => {
  it('should create an instance', () => {
    const directive = new NgElsesDirective();
    expect(directive).toBeTruthy();
  });
});
